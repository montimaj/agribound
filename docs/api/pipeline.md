# Pipeline

The pipeline module is the main entry point for agribound. It orchestrates the full workflow: composite building, optional fine-tuning, delineation, post-processing, evaluation, and export.

::: agribound.pipeline
